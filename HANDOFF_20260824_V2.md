# SpchConvSti 论文实验项目完整交接文档（V2）

> 更新时间：2026-08-24（Asia/Shanghai）。本文件替代旧版 `HANDOFF_20260824.md`。新任务应先读本文，再对服务器做只读实时检查；不得以论文之前的旧代码反推当前方法。

## 1. 不可违反的约束

- 论文方法以 `D:\硕士\U盘备份\正文存档.docx` 为权威来源；未经用户要求不得修改。
- 若实验要求正文改动，必须在对话中指出具体位置、问题和建议改法。
- 所有过程/完成报告均用中文，写入对应 `reports/`；关键异常必须同时在对话中说明报告路径。
- 服务器语音不得下载到本地。
- 冲突标签只有 `Conflict`、`Consistent`；正式使用版本 B，不增加第三类。
- 不虚填结果；完整且复核通过的表格才可称为可直接使用。
- 两张 RTX 3090 是论文最终正式训练设置；当前冒烟测试不等于正式训练。

## 2. 环境

- SSH：`ssh root@192.168.3.1 -p 32061`
- 根目录：`/data/jzh/2026/task2`
- 本地镜像：`C:\Users\yeadj\Documents\ChatGPT\论文\server_stage`
- 2×RTX 3090 24GB，主机约 24GB RAM、无 swap；两个完整 EmoVoice-PP 并发会 OOM，因此 TTS 只占 GPU 0。
- 方法环境：`experiments/step02_tts_pilot/.venv`，torch 2.4.1+cu121、diffusers 0.27.2、transformers 4.43.4、accelerate 0.34.2。

## 3. 数据标签事实

- STICKERCONV 有效样本 12,930。
- `origin_anno` 是官方七类表情包情感：Happiness/Sadness/Anger/Surprise/Disgust/Fear/Neutral，不是文本情感。
- `emotion` 是表情包自然语言描述，可能与官方标签不一致；审计结果不得覆盖 `origin_anno`。
- 原数据无逐轮文本情感金标，也无符合本论文需求的文本—表情包冲突标签。
- 文本极性已由 Qwen3VL 盲判并裁决分歧，不使用 VADER/词典。
- 三极性映射：Happiness、Surprise→Positive；Sadness/Anger/Disgust/Fear→Negative；Neutral→Neutral。
- 版本 B：文本三极性与官方表情包三极性相同为 Consistent，否则为 Conflict；正负、正中、负中不匹配都属于 Conflict。
- Surprise 仅在极性映射中为 Positive；语音仍生成七类中的 surprised。

## 4. 正式数据已升级为 SHA-256 安全划分

正式路径（旧路径分组版不再用于正式实验）：

`experiments/step04_full_dataset/policy_variants/neutral_mismatch_hash_stratified_811/`

原因：旧版虽然目标图片路径跨集合为 0，但 3,554 个路径中只有 3,465 个唯一 SHA-256；76 个多路径同图组里有 33 个跨集合，形成字节级目标图像泄漏。

新划分以图像 SHA-256 为不可拆分 group，并按“官方七类×版本B二类”14 单元分层。独立复核通过：

- 总量 12,930；Train/Validation/Test=10,344/1,294/1,292；
- sample_id、image_path、image SHA-256 三种跨集合重叠均为 0；
- 三集合合计 42 个分层单元全部非 0；
- Conflict=4,270，Consistent=8,660；
- 相对旧划分有 4,429 个样本移动。

关键报告：

- `step04_full_dataset/reports/HASH_SAFE_RESPLIT.md`
- `step04_full_dataset/reports/STATISTICS_TABLE_HASHSAFE.md`（完整可用 LaTeX 表格）
- `step04_full_dataset/reports/PAPER_CHANGE_NOTICE_20260824.md`（精确正文修改通知）
- `step11_feature_preprocessing/reports/PROCESS.md`
- 机器复核：正式目录下 `split_summary.json`、`verification_summary.json`

论文需要告知用户的修改：数据划分表述应改成“对目标表情包文件计算 SHA-256，字节内容相同的图片统一分配至同一集合”。统计总划分未变，但情感单元格使用 hash-safe 表格。

此外，本地 DOCX 的 Dataset Construction 仍写 RAVDESS/CREMA-D 作为正式参考声线，必须改成实际采用的 EmoVoice-PP 域内中性参考、split 间参考声线隔离；图像 512×512 句子应明确白底、保持比例、padding；模块二有 `SSubsequently` 拼写错误。具体替换句见上述正文修改通知。

## 5. 全量 EmoVoice-PP 语音

正式情感取官方七类表情包标签。文本按句界切分，每段最多 55 词；12,930 样本共 24,781 段；拼接时段间 180ms 静音，最终 16kHz 单声道。

旧路径分组任务在 87 个原始分段时因泄漏发现而停止。旧音频原样保留，仅用于审计，不删除、不复用到正式数据。

新的 hash-safe 正式任务：

- 状态/清单：`step04_full_dataset/artifacts/full_tts_pp_hashsafe/`
- 原始段：`step04_full_dataset/audio/full_tts_pp_hashsafe/decode/pred_audio/neutral_prompt_speech/`
- 16k 段：`.../audio/full_tts_pp_hashsafe/segments_16k/`
- 最终语音：`.../audio/full_tts_pp_hashsafe/final_16k/{train,validation,test}/`
- supervisor：`step04_full_dataset/scripts/full_tts_hashsafe_supervisor.sh`

SQLite/WAL 断点、原文件扫描、最多 3 次重试、文件锁和原子拼接均已启用。每批 100 段结束后才 reconciliation，所以首批期间 `status.json` 仍可能显示 0；须同时看原始 WAV 数及进程。不得启动第二个 EmoVoice-PP，也不得 scp 音频到本地。

## 6. 方法实现状态

### step07：论文数学核心

`experiments/step07_method_stage1/`

- 模块一：WavLM+OpenSMILE 残差适配、双向 InfoNCE、Whisper 时间戳帧映射、词级对齐、全局/局部冲突、上下文补全与仲裁、Transformer 联合表示。
- 模块二核心：内容/情感双流、VQ 风格码本、L_code、CA-FM。
- 模块三核心：Tweedie 端点、sem/emo/atm 奖励、动态权重、Constant-Noise 梯度、轨迹修正。
- 损失：L_align、L_content、L_code、第一阶段总损失、第二阶段 margin preference。
- 13 项 CPU 回归和 12,930 数据接口检查已通过。
- 二分类 Conflict 标签只用于划分/分组评测，不作为模块一额外 BCE 监督。

### step08：真实 diffusers U-Net 适配

`experiments/step08_method_stage2/`

- 内容向量投影为 cross-attention tokens，可与基础 tokens 拼接；
- CA-FM 用严格 Tensor hook 默认挂到 `mid_block`；
- 零初始化保持原 U-Net 行为；
- fake U-Net 8 项测试、小型真实 diffusers U-Net 测试均通过。
- SD1.5 核心权重已通过 `hf-mirror.com` 完整下载；完整 U-Net 的 FP16 测试通过，零初始化最大差 0、全部条件梯度正常，峰值显存约 2409.47 MiB。结果：`artifacts/sd15_full_smoke.json`。

### step09：真实 DDIM scheduler 适配

`experiments/step09_method_stage3/`

- 已接入 diffusers 0.27.2 DDIMScheduler；训练时间索引与推理位置分离；
- eta=0 时与官方 `DDIMScheduler.step().prev_sample` 完全一致，最大差 0；
- 激活轨迹修正后路径变化且梯度有限。
- 正式 `E_lat`/Latent CLIP 仍未部署，不能称模块三效果完成。

### step10：训练工程

`experiments/step10_training_engine/`

- DDP、NCCL/gloo、rank seed、fp32/fp16/bf16 AMP、GradScaler、裁剪；
- 原子 checkpoint，含 model/optimizer/scheduler/scaler/epoch/step/config/RNG/latest；
- CPU 单进程 10 项恢复测试通过；
- `torchrun --nproc-per-node=2` 双进程 gloo 测试通过，两 rank 更新后参数逐元素一致；
- 正式双 GPU/NCCL 压测等待 TTS 完成，不能与 GPU0 TTS 抢资源。

### step11：图像/VAE 预处理

`experiments/step11_feature_preprocessing/`

- 3,554 图像全部存在且可解码；发现并推动修复 SHA 泄漏；
- VAE cache 脚本已实现：RGBA 白底、等比 pad 512、posterior mode、0.18215、SHA 文件名、原子/断点；
- hash-safe manifest 再次验证跨集合图像哈希为 0；VAE latent 已完成并独立复核：3,465 个唯一哈希全部有效，失败 0，目录 `artifacts/vae_latents_hashsafe/`，约 123 MB。
- `artifacts/training_index_hashsafe/` 已覆盖 12,930 样本并全部关联 latent；语音尚未拼接完成，所以当前 training_ready=false，待 TTS 批次结算后可原子刷新。

### step12：第一阶段端到端训练步

`experiments/step12_integrated_training/`

- 已连通模块一→双流/VQ→DDPM 前向加噪→真实 diffusers U-Net/CA-FM→L_content+L_code+L_align；
- 小型 U-Net 和完整 SD1.5 U-Net 均测试通过；完整权重 batch=2 FP16 峰值显存约 3137.21 MiB，条件分支梯度正常且冻结 U-Net 无梯度；
- 当前仍为合成张量接口验证，不代表真实数据效果或正式训练。

### step13：评价聚合框架

`experiments/step13_evaluation_framework/`

- 已实现预测唯一性、正式集合归属、字段和有限值检查，默认要求 split 100% 覆盖；
- 支持 Overall、Consistent/Conflict、七类情感、七类×二类聚合；
- 固定随机种子的 bootstrap 95% 置信区间，输出 JSON/CSV；合成回归测试通过；
- 只完成聚合层，EGA/VQ-a/CUA/DCA 的逐样本计算仍需按正文定义实现，合成值不得写入论文。

## 7. 论文尚未明确、代码当前采用的工程选择

以下均未修改正文：融合 Transformer 使用融合种子 token+语义/语音/冲突 token；仲裁 MLP 用 Sigmoid；CA-FM 零初始化；训练期保留对齐投影头、推理期可绕过；外层 L_code 权重与量化器 commitment 权重分开；CA-FM 暂只挂 mid_block；图像 RGBA 合成白底、等比 pad 512。若写进正文需先向用户提出具体改法。

## 8. 无需完整语音仍可继续

- 以已完成的 hash-safe VAE latent cache 构建正式训练 Dataset/DataLoader；
- 选择并部署正文对应的真实 E_lat/Latent CLIP；
- 两阶段冻结/解冻与消融配置；
- 第二阶段正负偏好采样器（规则必须与正文确认）；
- 自动评价与按 Consistent/Conflict、七情感聚合框架；
- 基线骨架和统一推理接口；
- 少量正式语音完成后做 Whisper/WavLM/OpenSMILE 工程连通测试。

## 9. 必须等待正式语音/质检

- 全量 Whisper 词级时间戳、WavLM、OpenSMILE 与真实词级对齐缓存；
- 正式两阶段训练；语音相关基线公平对比；最终自动/人工指标、消融、案例与论文结果表；
- `test_human` 的真实参与者录音与人工核验，不能程序伪造。

## 10. 新任务启动检查顺序

1. 检查 hash-safe TTS 进程、原始 WAV 数、`status.json`、supervisor 日志；不要看旧 `full_tts_pp` 误判。
2. 读取 `step11_feature_preprocessing/artifacts/vae_latents_hashsafe/cache_report.json` 和 `verification_report.json`；SD1.5 下载、VAE cache 和完整权重 U-Net 测试已经完成，不要重复执行。
3. 阅读 `HASH_SAFE_RESPLIT.md`、step08–step12 各 `reports/PROCESS.md`。
4. 继续前先报告任何失败、泄漏或需要正文调整的结论。

## 11. 禁止重复踩坑

- 不把 `origin_anno` 当文本情感；不用自然语言 `emotion` 覆盖官方标签。
- 不恢复 VADER/refined 旧流程；不增加第三冲突类。
- 不把 Surprise 语音生成成 Positive。
- 不把冒烟测试写成论文最终结果。
- 不按路径而忽略同图异路径；正式数据只用 hash-safe 目录。
- 不复用旧 split 下的声线隔离音频。
- 不同时启动两个完整 TTS，不下载服务器音频，不擅改论文/表格结构。
