# SpchConvSti 项目交接文档（2026-08-29）

> 本文可完整复制给新对话。所有结论以论文为准，旧代码只作为参考；未经用户明确同意，不得修改论文 DOCX。

## 1. 项目目标与服务器

用户在撰写 SpchConvSti 论文，需要完成：数据集构建/分析、论文方法实现、对比实验和案例分析。服务器连接命令：

```bash
ssh root@192.168.3.1 -p 32061
cd /data/jzh/2026/task2
```

论文源文档（只读，用户自己修改）：`D:\硕士\U盘备份\正文存档.docx`。

用户要求：

1. 每一步在对应服务器目录写中文 Markdown 报告；若报告里有重要信息，必须在对话中点名具体报告路径。
2. 不要把后续全量语音下载到本地电脑。
3. 不得自行修改论文；发现需修改之处，说明具体位置和建议改法后等待用户决定。
4. 不要把“代码框架/合成冒烟通过”说成“正式训练或实验完成”。
5. 用户希望先完成所有不依赖完整语音的模型实现；语音生成应独立后台运行，不应阻塞其他工作。

## 2. 已确认的论文/数据构造规则（不能擅自改变）

### 2.1 情感与冲突标签

- 表情包七类情感使用原始数据集的官方 `origin_anno`：Happiness、Sadness、Anger、Surprise、Disgust、Fear、Neutral。
- 语音生成使用上述**七类表情包情感**，不是正/负/中性极性。
- 为生成“文本–表情包情感冲突”标签，文本极性由 LLM 判断，表情包极性由官方七类标签映射得到；Surprise 按用户决定归为 Positive。
- 用户最终使用 **版本 B**：文本极性与表情包极性不同即 Conflict，其中 Positive–Neutral、Negative–Neutral 也算冲突；相同则 Emotion-Consistent。只保留“一致/冲突”两类，不增加第三类。

### 2.2 第二阶段正负配对（用户已明确）

对 batch 内第 `i` 个样本：它自己的 sticker latent 是正样本；该 batch 内所有 `j != i` 的其他 sticker latent 都是负样本。不要再根据七类情感臆造正负配对。

## 3. 已完成工作（已真实产物，不是计划）

### 3.1 正式数据集清单与划分

正式版本目录：

```text
/data/jzh/2026/task2/experiments/step04_full_dataset/policy_variants/neutral_mismatch_hash_stratified_811/
```

- 总样本：12,930；Train/Val/Test：10,344 / 1,294 / 1,292。
- 用图像字节 SHA 分组后划分；已检查跨 split 的相同图像 SHA 为 0。
- 正式统计表已计算。用户采用版本 B；Happiness、Sadness 等类别间的数量差异来自原始 STICKERCONV 分布，不要伪造均衡。
- 旧的 LaTex 表曾给过用户；以后若重算统计，必须保证“先按一致/冲突，再在各类内分 Train/Val/Test”的表结构自洽。

### 3.2 方法代码骨架与单元/合成冒烟

以下均已实现并做过 CPU 或合成连通性测试，但**尚未进行正式真实数据训练**：

| 模块 | 状态 | 位置 |
|---|---|---|
| Stage 1：文本、声学/韵律适配、词级时间对齐、InfoNCE | 代码骨架与合成测试完成 | `experiments/step07_method_stage1/` |
| Stage 2：双流、VQ、CAFM | 代码骨架与合成测试完成 | `experiments/step07_method_stage1/` |
| Stage 3：Tweedie、奖励头、动态权重 | 代码骨架与合成测试完成 | `experiments/step07_method_stage1/` |
| SD 1.5 U-Net adapter | 实现并测试通过 | `experiments/step08_sd_adapter/` |
| DDIM | 实现；`eta=0` 一致性测试通过 | `experiments/step09_ddim/` |
| DDP/AMP/checkpoint | CPU/gloo 冒烟通过 | `experiments/step10_training_runtime/` |
| VAE sticker latent 缓存 | 3,465 个唯一 SHA，`[4,64,64]`；覆盖索引 12,930 行 | `experiments/step11_vae_latents/` |
| 合成端到端 SD1.5 | 通过 | `experiments/step12_end_to_end/` |
| 指标汇总与 bootstrap 框架 | 已实现，尚未做正式模型评价 | `experiments/step13_results_aggregation/` |

### 3.3 第二阶段“自身正、批内其余负”训练器

已实现并在服务器通过冒烟测试：

```text
/data/jzh/2026/task2/experiments/step15_preference_stage/src/spchconvsti_preference/trainer.py
/data/jzh/2026/task2/experiments/step15_preference_stage/scripts/smoke_test_in_batch_trainer.py
```

它会输出 `[B,B,3]` 分数矩阵：对角线是自身正样本，非对角线是批内负样本，并计算三维 margin loss。最近服务器测试输出：`score_matrix_shape=[3,3,3]`，loss 有限、投影头可反传。

报告：`/data/jzh/2026/task2/experiments/step15_preference_stage/reports/PROCESS.md`。

### 3.4 评价标注与自动汇总的准备

`experiments/step16_metric_scoring/` 已有自动标准化/结构检查脚本，只用于整理人工或模型给出的 EGA、VQ-a、CUA、DCA 标注；**没有产生任何正式分数**。用户曾指出不应在模型尚未完善前优先做评价，因此后续优先模型，不要扩展评价工作。

## 4. 正在做什么（以本次核查为准）

### 4.1 全量语音生成：后台自动运行，暂不人工干预

目录：

```text
/data/jzh/2026/task2/experiments/step04_full_dataset/artifacts/full_tts_pp_hashsafe_v2/
/data/jzh/2026/task2/experiments/step04_full_dataset/audio/full_tts_pp_hashsafe_v2/
```

最新状态（2026-08-29 本次核查）：

- 短分段总数 59,659；成功 4,290；仍可重试 55,369；耗尽重试 0。
- 12,930 条正式样本中，已完整拼接 2,159 条。
- 当前一个 EmoVoice-PP 推理批次正在运行（`run_emovoice_pp_batch.sh` / `inference_tts.py`）。
- 该批次结束后，已启动等待脚本 `launch_tts_v2_after_current.sh`；它将自动接力 v2 supervisor：batch=16、最多重试 100 次、异常等待 60 秒后恢复。普通模型退出或单段失败不会从头生成。

已知质量情况：早期 EmoVoice 输出情感弱、语速偏慢；调整后略有改善。用户同意先按当前方案继续完成后续数据，时间充足时再进一步调优。不要每个样本保留三份冗余音频。

预计：按已观察吞吐，剩余约 29–35 天（单 GPU、当前模型配置），这是粗略估计；服务器重启、存储/GPU 服务异常会拉长时间。用户要求在音频完成前不再反复检查它；除非用户主动要求或出现明确异常，不要让 TTS 阻塞其他工作。

报告：`/data/jzh/2026/task2/experiments/step04_full_dataset/reports/TTS_AUTONOMOUS_CONTINUATION_20260829.md`。

### 4.2 真实音频特征提取：当前失败，尚未跑通

目标是按论文 3.1.1 用少量已完成语音验证：Whisper 词级时间戳、RoBERTa 当前文本/上下文、冻结 WavLM 第 6 层、OpenSMILE eGeMAPSv02 LLD。

目录：`/data/jzh/2026/task2/experiments/step14_real_audio_smoke/`。

已修复：Whisper 不再依赖服务器 ffmpeg；样本会优先选短文本；脚本支持 `--limit` 单样本和耗时/完整异常记录。

**最新真实状态：单样本任务已失败，当前没有有效特征输出。**失败原因不是模型结构，而是加载 `microsoft/wavlm-base-plus` 时 Hugging Face 网络连接被重置，缓存不完整，程序尝试联网补模型文件失败。下一步应先检查/补齐 WavLM 与 RoBERTa 缓存，再用 `local_files_only=True` 固定离线加载，并重新跑单样本。不要把这个任务称为“正在运行”。

报告：`/data/jzh/2026/task2/experiments/step14_real_audio_smoke/reports/PROCESS_UPDATE_20260829.md`。

## 5. 仍未完成的工作与正确优先级

### A. 现在可继续、且不需要完整 12,930 条音频

1. 修复并完成 step14 单样本/14 样本真实特征提取，验证真实张量形状和数据接口。
2. 将真实特征接入已完成的 Stage 1/Stage 2 训练入口，先做每类少量样本的运行测试；该测试不可作为正式实验。
3. 完善 Stage 2 的实际训练入口（优化器、冻结第一阶段、checkpoint 恢复），但不得伪造 `E_lat`。
4. 梳理对比方法的可运行清单、数据接口和实验配置，但先不要跑正式对比实验。

### B. 当前明确受外部条件阻塞的部分

论文要求的预训练 `E_lat`（Latent CLIP）权重并不是 Stable Diffusion/Diffusers 自带权重。已查到论文来源为 Jason Becker 等的 *Controlling Latent Diffusion Using Latent CLIP*（arXiv:2503.08455），但此前未找到可验证的官方公开代码/权重。现有实现只保留 `latent_encoder` 接口和合成占位测试。

**不能**擅自把它换成普通 CLIP、VAE decode 后 CLIP，或从头训练；三者都会偏离论文。若找不到官方权重，需要先告诉用户并由用户决定：获取作者权重，或修改论文方法。

### C. 必须等完整语音数据后才做的正式工作

1. 按 Train/Val/Test 正式划分执行 Stage 1 训练。
2. 用完成的 Stage 1 权重训练 Stage 2（并接入真实 `E_lat`）。
3. 完整模型训练、推理、消融实验、对比实验、统计显著性和正式结果表格。
4. 生成结果的案例分析与论文实验章节最终填写。

## 6. 权重与训练：避免再次混淆

- RoBERTa、WavLM、Whisper、Stable Diffusion 的基础权重可下载并加载；WavLM 按论文冻结。
- EmoVoice-PP 已有用于语音生成的预训练检查点。
- 论文新建的 adapter、CAFM、VQ、投影头、奖励头、扩散条件模块等**没有默认权重**，必须在本项目数据上训练。
- 因此“代码实现完成”不等于“模型已训练”。完整语音出现前可做小样本连通测试，正式训练需等完整数据。

## 7. 文档一致性提醒（只提醒，不直接改论文）

此前已提醒用户并由用户处理过 GPU 数量、数据集比例、`test_human_1200` 表述。仍需在最终定稿前核验：

1. EmoVoice 引用/描述不得写成实际未使用的 RAVDESS/CREMA 流程；当前实际为 EmoVoice-PP 和域内中性参考音频。
2. Latent CLIP 引文信息需核验：论文中旧条目曾写为 Yang / arXiv:2502.11146；查到的论文记录是 Jason Becker 等 / arXiv:2503.08455。修改前须征得用户同意。
3. 数据集冲突策略只应写为目前已定的“一致/冲突”二类与版本 B 判定，不能写为三分类。

## 8. 常用检查命令

```bash
# 查看 TTS 进度
cat /data/jzh/2026/task2/experiments/step04_full_dataset/artifacts/full_tts_pp_hashsafe_v2/status.json

# 查看 TTS/真实特征后台进程
ps -ef | grep -E 'full_tts_hashsafe_v2|run_emovoice|inference_tts|extract_real_features' | grep -v grep

# 查看真实特征失败日志
tail -80 /data/jzh/2026/task2/experiments/step14_real_audio_smoke/artifacts/one_item.log
```

## 9. 新对话的建议开场指令

```text
请阅读我提供的 SpchConvSti 项目交接文档。不要检查或等待后台 TTS，优先继续“完整音频生成前可完成的模型实现”。先修复 step14 的 WavLM/Roberta 离线缓存问题，跑通单样本真实特征提取；每一步写中文 Markdown 报告到对应服务器目录，并在对话中指出重要报告。不要修改论文 DOCX，也不要自行用普通 CLIP/VAE 替代论文要求的 E_lat。
```
