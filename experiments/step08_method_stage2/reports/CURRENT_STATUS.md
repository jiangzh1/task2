# 模块二真实集成当前状态

## 已完成

- 内容条件到 U-Net cross-attention token 的适配；
- CA-FM 的可配置 hook、零初始化恒等行为与梯度测试；
- 8 项 fake U-Net 测试全部通过；
- 小型真实 diffusers U-Net 的前向、形状、有限值、内容梯度与 CA-FM hook 测试通过；
- SD1.5 核心组件的断点下载、文件锁、最大 12 次重试入口。

## 完整 SD1.5 测试

核心权重已完整下载并通过必需文件检查。完整 SD1.5 U-Net 在 GPU 1、FP16、`[1,4,64,64]` latent 下测试通过：输出有限，CA-FM 零初始化与原 U-Net 的最大绝对差为 0；内容、情感、冲突输入及 CA-FM 参数均获得有限梯度；峰值已分配显存约 2409.47 MiB。

模块二的完整权重工程集成已完成。尚未完成的是正式训练与不同 U-Net 挂载层级的消融，这些属于后续实验结果而非接口缺失。
