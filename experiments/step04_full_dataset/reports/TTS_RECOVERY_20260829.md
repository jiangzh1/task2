# 全量语音生成恢复报告（2026-08-29）

## 当前结论

全量语音生成没有完成，但断点任务已经恢复并正常运行。恢复过程不会删除任何已有音频，也不会重复生成状态数据库中已经成功的分段。

## 恢复前状态

- 目标分段数：24,781；成功分段数：4,137；完整样本数：2,096 / 12,930。
- 上次中断日志记录了 `cuCtxSetCurrent ... res=4`。这是服务器 HAMI/GPU 上下文异常，导致推理进程退出；不是数据格式、标签或 EmoVoice-PP 模型整体不可用。
- 另有少量分段出现“推理正常结束但未产生对应音频”。这些分段已保留失败记录，后续会在重试上限内再次进入队列。

## 已执行恢复措施

1. 使用 SQLite 状态库读取每个分段的成功、待重试和尝试次数；不清空既有输出目录。
2. 重新核对已有 raw、16 kHz 分段与拼接成品，恢复出 82 个 16 kHz 分段和 41 个完整样本。
3. 以 `MAX_ATTEMPTS=4` 重新启动监督脚本，使此前达到 3 次尝试上限但未产出音频的分段获得一次额外重试；其他未处理分段按原队列继续。
4. 推理在 GPU 0 运行；已完成结果与状态数据库会持续落盘，进程再次意外退出时可从最新状态继续。

## 2026-08-29 检查结果

- 监督脚本与 `inference_tts.py` 均存活。
- 成功分段：4,219 / 24,781；可重试分段：20,562；达到当前上限的分段：0。
- 完整样本：2,137 / 12,930。
- GPU 0 正在占用约 4.8 GiB；GPU 1 空闲，可用于不干扰生成的小规模方法验证。

## 风险与处理

HAMI 的 CUDA 上下文问题仍可能再次发生，但不会造成已完成音频丢失或前功尽弃：恢复脚本保留状态、锁和原子写入的文件。若再次中断，只需检查状态后续跑；正式完成前不把未生成样本标记为可训练。
