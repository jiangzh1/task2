# 全量文本极性推理：烟雾测试报告

## 测试设置

从官方 Validation 清单的前 5 条记录中抽取文本与最多 4 条最近历史上下文。两个模型均看不到表情包、图片文件、`origin_anno` 或任何原有冲突标签。

| 模型 | 服务端口 | 固定显卡 | 已完成 | 异常 |
|---|---:|---:|---:|---:|
| Gemma3 12B | 11434 | GPU 0 | 5 | 0 |
| Qwen3 8B | 11435 | GPU 1 | 5 | 0 |

两套 Ollama 服务的 `CUDA_VISIBLE_DEVICES` 分别为 0 和 1，能够并行运行。每一条推理结果都即时追加写入 JSONL；进程意外停止后，脚本会跳过已成功的样本并从未完成处继续。

## 结论

文本极性输出均通过 JSON 格式校验，且 Qwen3 的结构化结果可从其 `thinking` 字段正确解析；因此可进入全量第一轮盲推理。

这只是软件与服务连通性验证，5 条样本不用于统计模型一致率、数据集比例或论文表格。
