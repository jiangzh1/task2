# Git 分支与归档说明（2026-08-30）

为保留 SD 1.5 已实现代码并开展 SDXL 迁移，建立独立的论文归档仓库和两个分支：`sd15-baseline` 保存迁移前基线，`sdxl-work` 用于后续迁移。在线运行的原目录 `/data/jzh/2026/task2` 不在本次操作中修改。

归档范围包括论文自有的代码、配置、过程报告、正式数据集划分清单及 StickerConv 精炼 JSONL 数据。排除语音与图像文件、特征缓存、检查点/模型权重、虚拟环境、`data/SER_Dataset`、处理过程数据、Parquet 文件和第三方 EmoVoice 源码/资源。

由于每个入库 JSONL 均低于 GitHub 单文件 100 MB 限制，论文相关的数据清单与原始精炼 JSONL 可以随代码一同提交；后续若新增单文件达到或超过该限制，应改用 Git LFS 或仅提交可重建的清单。
