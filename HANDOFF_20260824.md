# SpchConvSti 论文实验项目完整交接文档

> 更新时间：2026-08-24（Asia/Shanghai）  
> 用途：可将本文件全文直接发给新的 Codex 聊天。新聊天应先阅读本交接，再检查服务器实时状态；不要从旧代码反推论文方法。

## 1. 用户目标与最高优先级约束

用户正在完成一篇语音驱动表情包生成论文的实验部分，项目目标包括：

1. 数据集构建与分析；
2. 情感语音生成和质量控制；
3. 论文方法实现；
4. 对比实验、消融实验；
5. 自动与人工评测；
6. 案例分析和论文表格填充。

必须遵守：

- **论文正文是方法实现的唯一权威来源。** 服务器旧代码产生于论文之前，可以忽略，不能用旧代码覆盖论文设计。
- 论文只读副本：`D:\硕士\U盘备份\正文存档.docx`。
- 未经用户明确要求，不修改论文 DOCX。若实验发现必须修改正文，先在对话中指出具体位置、原句问题和建议改法。
- 所有实验过程报告和完成报告必须使用中文，并放入对应实验目录的 `reports/`。
- 若有用户必须知道的重要信息，不能只写进报告；需在对话中明确指出具体报告路径和结论。
- 语音文件只保存在实验服务器，**不得下载到本地电脑**。
- 不虚填论文实验结果。表格只有完全确定后才能称为可直接使用；未完成表格不要擅自改结构。
- 输出 LaTeX 表格时必须保留正常换行，方便复制。
- 当前正式冲突标签始终只有二类：`Conflict` 和 `Consistent`，不得增加第三类。

## 2. 服务器与工作目录

- SSH：`ssh root@192.168.3.1 -p 32061`
- 服务器项目根目录：`/data/jzh/2026/task2`
- 本地同步工作区：`C:\Users\yeadj\Documents\ChatGPT\论文\server_stage`
- 服务器有 2 张 RTX 3090（24 GB/张）。
- 主机内存约 24 GB，无 swap。
- 两个完整 EmoVoice-PP 进程并发曾导致主机 OOM，因此当前只允许一个 TTS 进程使用 GPU 0。
- 最终论文正式训练计划使用两张 RTX 3090；当前 CPU/单卡冒烟测试不属于论文正式训练设置。

## 3. 数据源核验结论

### 3.1 STICKERCONV 官方数据

- 有效任务样本：12,930 条。
- 官方原划分：10,785 / 1,000 / 1,145，但当前正式版本已经重新划分。
- `origin_anno` 是官方七类**表情包情感标签**：
  - Happiness
  - Sadness
  - Anger
  - Surprise
  - Disgust
  - Fear
  - Neutral
- 原数据没有逐轮文本情感金标。
- `emotion` 字段是表情包情感的自然语言描述，可能与 `origin_anno` 不一致。项目审计不会用它覆盖官方 `origin_anno`。
- STICKERCONV 原始构建流程并未提供符合本项目需求的“文本—表情包冲突标签”。本项目自行构建二分类冲突标签。

### 3.2 文本极性

- 文本极性不是人工词典或 VADER 结果。
- 已使用大模型进行盲文本极性判断，并对分歧样本进行裁决。
- 正式数据字段中可见：`text_polarity_source = Qwen3VL_blind_adjudication`。
- 官方表情包标签没有被大模型审计结果覆盖。

### 3.3 极性映射

- Happiness → Positive
- Surprise → Positive（用户已明确决定）
- Sadness / Anger / Disgust / Fear → Negative
- Neutral → Neutral

注意：Surprise→Positive 只用于构建三极性冲突标签；生成语音时仍使用七类中的 `Surprise/surprised`，不能生成成笼统 Positive。

## 4. 当前正式数据版本：版本 B

正式路径：

`/data/jzh/2026/task2/experiments/step04_full_dataset/policy_variants/neutral_mismatch_stratified_811/`

正式规则：

> 将当前文本极性和官方表情包情感映射为 Positive、Negative、Neutral；两者不同即为 Conflict，相同即为 Consistent。

因此版本 B 的 Conflict 包含：

- 正—负；
- 正—中性；
- 负—中性。

正式划分采用“官方七类表情包情感 × 二分类冲突标签”分层，并以目标表情包图片分组，约 8:1:1：

| Split | Samples |
|---|---:|
| Train | 10,344 |
| Validation | 1,294 |
| Test | 1,292 |
| Total | 12,930 |

核验结论：

- 12,930 个 sample_id 全部唯一；
- 3,554 张目标表情包跨划分重复为 0；
- 14 个非空“七类情感 × Conflict/Consistent”组合在三个划分中均非零；
- Conflict 4,270，Consistent 8,660；
- 官方表情包标签分布：Happiness 8,556，Neutral 1,847，Sadness 1,491，Anger 573，Surprise 271，Fear 164，Disgust 28。

版本 A（只有正—负算 Conflict）仍保留在：

`policy_variants/strict_pn/`

但用户当前明确使用版本 B。除非用户要求切换，不要改变正式版本。

关键报告：

- `experiments/step04_full_dataset/reports/VERSION_B_STRATIFIED_FINAL.md`
- 本地对应：`server_stage/step04_full_dataset/reports/VERSION_B_STRATIFIED_FINAL.md`

## 5. 全量情感语音生成

### 5.1 已选择方案

- 模型：EmoVoice-PP。
- 早期 RAVDESS/CREMA-D 域外参考音频生成结果情感非常平淡，已停止作为正式参考方案。
- 官方 EmoVoice-PP 模型域内中性参考音频明显更好，用户认为“可以先用着完成后续实验，有时间再调”。
- 每个样本只生成一条正式音频，不再为每条重复生成三条。
- 语音目标情感严格取 `sticker_emotion_official` 七类标签，不取三极性。

情感映射：

- Happiness → happy
- Sadness → sad
- Anger → angry
- Surprise → surprised
- Disgust → disgusted
- Fear → fearful
- Neutral → neutral

### 5.2 超长文本与断点续跑

版本 B 文本中位数约 73 词，90% 分位约 104 词。为避免 EmoVoice-PP 的 3,000 token 上限造成隐性跳过：

- 按句子边界切段；
- 每段最多 55 个英文词；
- 共 12,930 个样本、24,781 个生成分段；
- 同一样本全部分段使用相同情感提示和参考声线；
- 最后按原顺序拼接，段间插入 180 ms 静音；
- 输出 16 kHz 单声道 WAV。

断点机制：

- 每个分段生成后立即写 22.05 kHz 原始 WAV；
- SQLite/WAL 保存状态；
- 重启时扫描已有文件，只补缺失内容；
- 每个缺失分段最多重试 3 次；
- supervisor 使用文件锁防止重复启动；
- SSH 和本地电脑断开不影响 `nohup` 后台任务；
- 模型异常退出时保留现场并停止，避免反复重载导致 OOM。

### 5.3 当前运行状态（交接快照）

- 最新只读检查：原始分段已落盘 67 个。
- `segments_16k=0`、`final_16k=0` 是因为当前 supervisor 每批 100 个结束后才统一转码、拼接和结算。
- `status.json` 在首批结束前仍显示 succeeded=0，这不表示没有生成；实时进度应同时查看原始 WAV 数。
- TTS supervisor 和 `inference_tts.py` 均正在运行。
- 首条 15.45 秒音频生成耗时约 34.66 秒，RTF 2.24。
- 全量单进程保守估计 8–12 天；首批 100 条结算后需用真实批次耗时重估。

状态命令：

```bash
cd /data/jzh/2026/task2
experiments/step04_full_dataset/scripts/full_tts_status.sh
```

若服务器重启或 supervisor 停止，可重新启动（已有结果不会重做）：

```bash
cd /data/jzh/2026/task2
nohup env BATCH_SIZE=100 MAX_ATTEMPTS=3 \
  experiments/step04_full_dataset/scripts/full_tts_supervisor.sh \
  > experiments/step04_full_dataset/artifacts/full_tts_pp/launcher.log 2>&1 < /dev/null &
```

关键目录：

- 清单和状态：`experiments/step04_full_dataset/artifacts/full_tts_pp/`
- 22.05 kHz 原始分段：`experiments/step04_full_dataset/audio/full_tts_pp/decode/pred_audio/neutral_prompt_speech/`
- 16 kHz 分段：`experiments/step04_full_dataset/audio/full_tts_pp/segments_16k/`
- 最终完整语音：`experiments/step04_full_dataset/audio/full_tts_pp/final_16k/{train,validation,test}/`
- 中文过程报告：`experiments/step04_full_dataset/reports/FULL_TTS_PROCESS.md`

禁止将这些音频 scp 到本地。

## 6. 论文方法正文与当前代码

论文只读来源：`D:\硕士\U盘备份\正文存档.docx`。Method 由三部分组成：

1. Context-Collaborative Intent Reasoning and Semantic Completion；
2. Conflict-Aware Conditional Feature Injection；
3. Consistency Evaluation and Generation Optimization。

方法代码：

`/data/jzh/2026/task2/experiments/step07_method_stage1/`

本地镜像：

`C:\Users\yeadj\Documents\ChatGPT\论文\server_stage\step07_method_stage1\`

### 6.1 已实现模块一数学核心

- `ProsodyEnhancedSpeechAdapter`：实现 WavLM A 与 OpenSMILE/BiLSTM R 的正文残差适配公式；
- `ProjectionHead`；
- 双向 InfoNCE `L_align`；
- Whisper 秒级时间戳到 WavLM 帧区间；
- 词级时间窗精确均值对齐；
- `Delta_global=e_y-e_s`；
- `d_i`、`sim_i`、`omega_i`、`Delta_local`；
- `Delta=[Delta_global;Delta_local]`；
- 当前文本查询上下文的 cross-attention；
- `alpha=MLP(c_bar)`、`Delta_tilde=alpha⊙Delta`；
- Transformer 联合表示 `h_joint`。

关键纠正：此前临时骨架错误地把数据集 Conflict/Consistent 标签加入冲突 BCE，现已删除。论文模块一按真实文本—语音特征构建冲突向量，训练损失使用双向 InfoNCE。数据集二分类标签用于划分、分组评测和分析。

### 6.2 已实现模块二独立数学核心

- 内容/情感双流投影；
- K 项可学习风格码本（K 是超参数，不能擅自永久固定为七类；冒烟测试 K=7）；
- VQ 最近邻、stop-gradient 与 `L_code`；
- `gamma_adapt`、`beta_adapt`；
- `F'=(1+gamma)⊙F+beta`；
- CA-FM 零初始化，初始为恒等映射。

未完成：真实 SD1.5 cross-attention 注入和 U-Net 层级 hook。

### 6.3 已实现模块三独立数学核心

- Tweedie `z0` 估计；
- sem/emo/atm 三个两层奖励投影头；
- 三种动态权重调度并验证和为 1；
- Constant-Noise Approximation（噪声预测显式 detach）；
- DDIM 临时状态；
- `eta_t` 梯度轨迹修正；
- 预训练 latent encoder 参数冻结，但保留对输入 latent 的梯度。

未完成：论文引用的真实 `E_lat`/Latent CLIP 模型部署与 diffusers scheduler 集成。

### 6.4 已实现损失

- `L_align`
- `L_content`
- `L_code`
- 第一阶段 `L_total`
- 第二阶段 margin preference loss `L_preference`

### 6.5 测试结果

- `artifacts/smoke_test_v2.json`：13 项 CPU 回归检查全部通过；
- `artifacts/dataset_interface_validation.json`：12,930 条正式样本接口校验通过；
- 测试覆盖时间窗均值、mask、局部权重归一化、仲裁范围、CA-FM 恒等初始化、VQ 索引、奖励权重、Constant-Noise 梯度、冻结 latent encoder 和反向梯度。

关键报告：

- `reports/FINAL.md`
- `reports/PROCESS.md`
- `reports/FORMULA_CODE_MAPPING.md`

## 7. 正文尚未明确、代码已作出的实现选择

这些选择没有修改论文，但用户必须知情：

1. 正文说“拼接三个向量后输入多层 Transformer”，没有定义序列维。代码使用一个拼接融合种子 token，加语义、语音、冲突三个组件 token，并取融合 token 作为 `h_joint`。
2. 正文未规定仲裁 MLP 的激活函数。代码使用 Sigmoid，将 `alpha` 限制在 `[0,1]`。
3. CA-FM 四组预测/投影层采用零初始化，使接入预训练 U-Net 时初始为恒等映射。
4. 正文一处说对齐训练后移除投影头，但联合训练总损失仍含 `L_align`。当前解释是训练期间保留投影头，推理时移除或绕过。
5. 正文外层 `lambda_1` 控制 `L_code`，而 `L_code` 内又有 `lambda_code` 控制 commitment 项。代码分别命名为外层 `lambda_code` 和量化器 `commitment_weight`。

若要修改论文以明确这些内容，必须先告诉用户具体修改建议。

## 8. 完整语音完成前仍可实现的任务

以下工作不要求全量语音，可继续推进：

### 优先级 A：建议立即做

1. **Stable Diffusion v1.5 集成**
   - 确定 diffusers 版本；
   - 将 `c_con` 注入 U-Net cross-attention；
   - 为选定 U-Net block 注册 CA-FM；
   - 用模拟 `h_joint/Delta` 做前向、梯度和恒等初始化测试。

2. **模块三真实管线集成**
   - 核对论文引用的预训练 `E_lat`/Latent CLIP 开源实现和许可证；
   - 接入真实 latent encoder；
   - 将轨迹修正接入 DDIM scheduler；
   - 用随机 latent 和一张服务器目标 sticker 做单步/多步测试。

3. **训练工程**
   - 双卡 DDP；
   - AMP/bfloat16 或 fp16；
   - 原子 checkpoint、断点续训、optimizer/scheduler state；
   - 随机种子、配置快照、实验日志；
   - 两阶段冻结/解冻策略；
   - 消融开关。

4. **文本与图像侧预处理**
   - 全量 RoBERTa 当前文本和上下文特征；
   - 目标表情包图片完整性、尺寸、模式和重复校验；
   - SD1.5 VAE latent 缓存；
   - 目标 sticker 路径和 sample_id 对齐核验。

### 优先级 B：可以搭框架

5. **第二阶段偏好样本构建接口**
   - 正样本为真实目标 sticker；
   - 负样本选择规则尚需固定，不能随意抽取后直接用于论文；
   - 可先实现可配置的同情感/异情感、同内容/异内容采样器和无泄漏检查。

6. **评价与消融框架**
   - 按 Consistent/Conflict 和七类情感分组聚合；
   - EGA、VQ-a、CUA、DCA 接口；
   - bootstrap 置信区间和随机种子；
   - 去语音、去上下文、去 Delta、去 CA-FM、去 VQ、去轨迹修正的配置开关。

7. **基线工程骨架**
   - Whisper+Qwen3-8B+SD1.5；
   - Whisper+Qwen3-8B+SDXL；
   - Qwen-Image；
   - MERaLiON-SER+SD；
   - SenseVoice+SD；
   - Omni-Diffusion、MACS。

模型下载和实际运行要考虑磁盘、网络、显存及论文公平设置；不要一次性并行加载多个大型模型。

### 可用少量现有音频做的测试

- 服务器已有 EmoVoice-PP 七类控制样本及正在生成的原始分段，可用于验证 Whisper/WavLM/OpenSMILE 预处理脚本的工程连通性；
- 这些小样本只能做管线测试，不能产生论文正式训练或指标结论；
- 不需要把音频下载到本地。

## 9. 必须等待完整语音或质检后的任务

- 全量 Whisper 转写与词级时间戳；
- 全量 WavLM 中间层特征；
- 全量 OpenSMILE LLD 和 BiLSTM 输入；
- 真实词级文本—语音对齐缓存；
- 模块一真实数据训练；
- 模块一与模块二正式联合训练；
- 模块三正式偏好训练；
- 所有语音相关基线的公平对比；
- 最终自动指标、消融结果和论文表格；
- 语音 ASR 可懂度、静音比例、情感可辨性和分层人工试听质检。

`test_human` 仍需要真实参与者录音与人工核验，不能用程序伪造。

## 10. 建议下一聊天的执行顺序

1. 先运行 `full_tts_status.sh`，确认 TTS 未停止；不要为了方法开发终止 TTS。
2. 阅读：
   - `step07_method_stage1/reports/PROCESS.md`
   - `step07_method_stage1/reports/FORMULA_CODE_MAPPING.md`
   - `step07_method_stage1/artifacts/smoke_test_v2.json`
3. 创建下一对应实验目录及中文 `reports/PROCESS.md`。
4. 优先实现 SD1.5 的双流条件注入和 CA-FM U-Net hook，先用模拟条件 CPU/GPU 1 小规模测试。
5. 再实现真实 latent encoder 与 DDIM correction 集成。
6. 最后补齐双卡训练工程、文本/图像预处理和评价框架。
7. 每完成一步写中文完成报告，并在对话中指出重要结论和路径。

## 11. 仍未完成的整体论文实验主线

- 全量语音完成与质检；
- 方法完整端到端集成；
- 两阶段正式训练；
- 全部基线；
- 自动与人工评测；
- 消融实验；
- 一致、冲突、失败案例分析；
- 论文最终表格和图；
- 145 条真实 `test_human`。

## 12. 禁止误判或重复踩坑

- 不要把 `origin_anno` 说成文本情感；它是表情包情感。
- 不要用表情包自然语言 `emotion` 描述覆盖官方标签。
- 不要将 Surprise 在语音生成时变成笼统 Positive。
- 不要增加第三种冲突类别。
- 不要恢复旧版 VADER 或 `refined` 脚本作为正式方案。
- 不要把小样本冒烟测试称为最终实验。
- 不要同时启动两个完整 EmoVoice-PP 进程。
- 不要下载服务器音频到本地。
- 不要修改用户论文或表格结构而不提前告知。
- 不要因为 `status.json` 首批为 0 就误判 TTS 没有运行；先数原始 WAV 并检查进程。
